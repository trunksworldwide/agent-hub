# Frontend will call the local Control API
VITE_API_BASE_URL=http://127.0.0.1:3737

# Control API reads/writes agent brain files from this workspace
# (Used by server/index.mjs)
CLAWD_WORKSPACE=/Users/trunks/clawd
